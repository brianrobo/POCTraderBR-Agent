# 도메인 개념: 물량 털기

`chart_agent/cv_breakout.py`가 실제로 잡으려는 패턴과, 코드의 임계값들이
왜 그렇게 정해졌는지에 대한 배경 설명. 코드 주석에도 일부 있지만, 전체
맥락은 여기에 모아둔다. 새로운 기준/튜닝 요청이 오면 먼저 이 문서에 이미
있는 개념의 연장인지 확인할 것.

이 문서는 전체 분석 로드맵([chart-analysis-framework 스킬](.claude/skills/chart-analysis-framework/SKILL.md)의
4개 항목) 중 **2번(세력이 물량을 털고 나갔는지 확인)**에 해당한다.

## 물량 털기란

세력(자금력 있는 매매 주체)이 특정 가격대에 보유 물량을 넘기기 위해
쓰는 수법 중 하나. 흐름은 대략 이렇다:

1. 가격을 큰 폭으로 올리면서 거래량(거래대금)을 크게 터뜨린다 — 개미
   투자자들의 매수를 유인.
2. 이후 가격이 그 자리 근처까지(특히 그 구간의 **고가/윗꼬리**, 단순
   종가가 아니라) 다시 한 번 접근하도록 만든다 — 소위 **전고 돌파**
   시도. 이때 그 가격대에 물려 있던 개미들이 "본전 근처"라고 생각하고
   물량을 던지게 된다.
3. 세력은 그 던져진 물량을 받아서(매집) 정리하고, 가격은 다시 하락한다.

즉 "가격 상승 + 거래량 폭증"은 시작점일 뿐이고, 그 뒤에 **같은 가격대로
재접근했다가 하락하는지**까지 확인해야 실제 물량 털기 정황에 가깝다.

이 프로젝트는 원래 "가격 상승+거래량 폭증"이라는 일반적인 이름을 쓰다가,
이 개념을 반영해 "물량 털기"로 용어를 통일했다 (커밋 `371cf89`).

## 왜 종가가 아니라 고가(윗꼬리)를 보는가

양봉이라도 종가는 시가보다 낮게 밀려서 마감할 수 있다 — 즉 하루 중
고가에서 매도세에 눌려 종가는 그보다 낮다. "가격 상단에서 변동성이
나오는 부분이 물량 터는 부분"이라는 설명대로, 실제 물량을 넘기는 지점은
**그날 찍은 최고가(윗꼬리 끝)** 쪽이다. 그래서:

- `detect_retest_and_reject()`의 기준 가격(`zone_level`)은 캔들 종가가
  아니라 그 구간에서 실제로 찍은 **최고가**(`range_top`, 픽셀 y좌표
  기준 최솟값)를 쓴다.
- 동그라미로 표시할 때도 몸통(`body_top`~`body_bottom`)이 아니라
  **윗꼬리까지 포함한 범위**(`range_top`~`body_bottom`)를 그린다.

## 한 구간에 여러 개의 유의미한 거래량이 있을 수 있다

제일 크게 터진 캔들 하나만 보면 안 된다 — 그 옆에 절반 정도 크기라도
여전히 눈에 띄는 거래량 캔들이 붙어 있으면, 가격이 그 구간(폭발 캔들의
시가) 아래로 확실히 무너지기 전까지는 전부 같은 물량 털기 구간으로
봐야 한다.

- `extend_cluster_to_breakdown()`: 구간의 끝을, 개별 신호 임계값이 아니라
  "가격이 폭발 캔들 시가 아래로 확실히 무너지는 지점"까지 늘린다.
- `find_significant_volume_points()`: 그렇게 늘어난 구간 안에서, 최대
  거래량의 약 35% 이상인 캔들을 **양봉/음봉 상관없이** 전부 찾아서 각각
  따로 표시한다 (바로 붙어있는 캔들끼리만 거래량이 큰 쪽으로 합침). 실제
  샘플 3개 구간 중 2개는 최대 거래량이 음봉에서 나왔다 — 처음엔 양봉만
  걸러서 놓치고 있었다 (커밋 `1c88d59`).

## 전고 돌파 후 재접근-하락 (재검증 중인 로직)

`detect_retest_and_reject()`는 위 구간의 최고가를 이후 캔들이 (한 번
눌렸다가) 다시 찍고, 그 캔들의 저가 밑으로 하락하는지를 확인해서 별도
신호(`재접근 후 하락`)로 표시한다. 다만 이 로직은 아직 실제 사례로 충분히
검증되지 않았다 — 샘플 차트에서는 허용 오차(±2%)로는 재접근이 잡히지
않았다. 튜닝하게 되면 [cv-detection-tuning 스킬](.claude/skills/cv-detection-tuning/SKILL.md)의
워크플로우(실제 캡처 + 시각적 확인)를 따를 것.

## 꼬리 인식은 색상이 아니라 밝기로

이평선 제거용 필터가 얇은 꼬리 선까지 같이 지워버리는 문제가 있었고,
그다음 시도(색상 임계값, 행 평균 밝기)도 안티에일리어싱으로 옅어진
1~2px 꼬리를 놓쳤다. 최종적으로는 캔들 x범위 안에서 **행별 최솟값
밝기**가 배경보다 확실히 어두운 동안 몸통 밖으로 계속 확장하는 방식
(`_refine_wicks()`)으로 해결했다 — 평균이 아니라 최솟값을 써야 캔들 폭
전체가 아니라 꼬리 그 자체(1~2px)의 신호를 잡을 수 있다.
