# 프로젝트 히스토리

커밋할 때마다 그 커밋에서 추가/변경된 핵심 기능을 날짜별로 기록합니다.
코드 diff 나열이 아니라 "무엇을 할 수 있게 됐는지" 관점으로 짧게 적습니다.
과거 항목은 수정하지 않고 계속 아래로 추가합니다 (최신이 위).

## 2026-09-12

- **구간 내 2차 거래량 급증도 함께 표시** (`9d1f6e6`) — 제일 큰 거래량 캔들 하나만 표시하던 걸, 같은 물량 털기 구간(가격이 폭발 캔들 시가 아래로 무너지기 전까지) 안에서 그 절반 정도 크기라도 눈에 띄는 거래량 캔들이 있으면 전부 각각 동그라미로 표시하도록 변경.
- **재접근-하락(물량 털기) 정황 탐지 추가** (`6a453e7`) — 거래량 폭증 캔들이 만든 고점('전고')을 이후 캔들이 다시 찍고 그 밑으로 하락하는지까지 확인. 발견되면 기존 노란 동그라미(폭증 지점)에 주황색 동그라미+연결선으로 재접근 지점을 표시. 캔들 이미지 경로만 지원 (캔들 JSON 경로는 아직 TODO). 표시하는 동그라미도 구간 전체가 아니라 실제 거래량 최대 캔들 하나에 딱 맞게 그리도록 변경.
- **"물량 털기" 용어로 통일** (`371cf89`) — 판단 기준/결과 문구를 일반적인 "가격 상승+거래량 폭증" 대신 세력의 물량 털기 의심 신호로 표현 (탐지 로직 자체는 변경 없음).
- **검출 동그라미 선 두께 축소** (`49976c1`) — 3px → 2px, 차트가 덜 가려지도록.
- **커밋 히스토리 로그 도입** (`f58446a`) — 이 `HISTORY.md`와, 커밋할 때마다 이걸 갱신하는 걸 표준 습관으로 만드는 스킬(`.claude/skills/update-history-log/`) 추가.
- **이미지 클릭 시 확대 모달** (`39aee04`) — 웹 대시보드에서 차트/검출 결과 이미지를 클릭하면 화면에 크게 띄워서 볼 수 있음. "분석하기"로 단일 카드 분석을 완료하면 자동으로 확대 모달이 열림.
- **검출 구간에 동그라미 표시** (`15a095e`) — OpenCV가 찾은 "가격 상승+거래량 폭증" 구간을 캔들/거래량 영역에 동그라미로 표시. 실제 사용자가 손으로 동그라미 친 차트와 위치가 일치함을 확인. CLI는 리포트와 함께 PNG로 저장, 웹은 결과에 이미지로 포함.
- **API 키 없을 때 CLI가 깔끔하게 실패** (`982bd1c`) — 트레이스백 대신 사람이 읽을 수 있는 안내 메시지로 종료.
- **OpenCV 기반 무료 실시간 판단 + 로컬 웹앱** (`3f50069`) — 일봉/30분봉/3분봉의 "가격 상승+거래량 폭증" 판단을 Claude 호출 없이 OpenCV로 직접 계산(무료, 즉시). 화면 캡처는 계속 Claude Vision 사용. 이미지/캔들 데이터(JSON) 입력을 공통 인터페이스(`ChartInput`)로 통일해서 나중에 증권사 REST API 연동 시에도 같은 파이프라인을 재사용 가능. FastAPI + 브라우저 대시보드(`webapp/`) 추가 — 로컬에서 이미지 업로드/붙여넣기로 바로 테스트 가능.
- **최초 스캐폴딩: 일봉/30분봉/3분봉/화면캡처 4개 분석 에이전트** (`4e9070c`) — 타임프레임별 판단 기준을 `config/criteria.yaml`에서 관리하고, Claude Vision으로 차트 이미지를 분석해 종합 리포트를 만드는 CLI 도구(`main.py`)의 기본 구조.
