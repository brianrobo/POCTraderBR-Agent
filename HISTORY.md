# 프로젝트 히스토리

커밋할 때마다 그 커밋에서 추가/변경된 핵심 기능을 날짜별로 기록합니다.
코드 diff 나열이 아니라 "무엇을 할 수 있게 됐는지" 관점으로 짧게 적습니다.
과거 항목은 수정하지 않고 계속 아래로 추가합니다 (최신이 위).

## 2026-09-13

- **분홍/마젠타 거래대금 테마 지원 + 재접근-하락 기능 제거** (`3287c76`, `4083849`) — "정배열" 등 다른 프리셋 화면에서 거래대금 막대가 보라 대신 마젠타(255,0,255)로 나오는 경우도 인식하도록 색상 범위 추가. 같은 화면에서 캔들과 같은 색의 전체 폭 수평 기준선(현재가 라인) 때문에 캔들 분리가 완전히 깨지는 버그도 발견해 수정(폭의 30% 이상 채우는 행은 지움). 재접근-하락(전고 돌파 후 하락) 표시는 검증 부족 + 사용자 요청으로 로직째 제거.

## 2026-09-12

- **매집 구간(로드맵 3번 항목) 1차 구현** (`b895b0b`, `b6663af`) — 각 물량 털기 구간 직전의 조용한(거래량 낮은) 구간을 하늘색 박스로 표시. 실제 샘플 차트로 여러 차례 검증: 장대양봉 자체는 제외, 병합 때문에 숨겨진 앞 캔들도 놓치지 않도록 처리, 거래량은 낮지만 가격 폭이 큰 캔들(예: 급락 캔들)은 제외, 최대 40개 캔들까지만 확장. DOMAIN.md와 chart-analysis-framework 스킬에 상세 규칙 반영.
- **범례 위치를 좌측 상단→우측 상단으로 이동** (`5fee22d`)
- **결과 이미지에 범례 추가** (`c39ef52`) — 이미지 맨 위에 "○ : 물량 털기" (재접근-하락 신호가 있으면 "○ : 재접근 후 하락"도 주황색으로) 표시. 한글이라 cv2.putText로는 안 그려져서 Pillow+맑은고딕 폰트로 렌더링.
- **chart-analysis-framework 스킬 추가** (`4dc6e0a`) — 이 도구가 최종적으로 판단할 4가지 큰 그림(세력 존재 확인/물량 털기 확인/매집 구간/이평선 정배열-역배열)을 로드맵으로 정리. 지금까지 만든 물량 털기 검출은 이 중 2번에 해당하고, 나머지 3개는 아직 방법론 미정 — 사용자가 하나씩 기준을 주면 같은 방식(cv-detection-tuning 워크플로우)으로 검증하며 채워나갈 예정.
- **cv-detection-tuning 스킬에 DOMAIN.md 동기화 체크 추가** (`5ebf06f`) — 검출 로직 커밋 절차에 "DOMAIN.md도 같이 맞는지 확인" 단계를 명시해서 방금 발견한 것 같은 문서-코드 불일치가 재발하지 않도록 함.
- **DOMAIN.md 최신화** (`576d32c`) — 음봉 포함 수정(`1c88d59`) 이후 문서가 "양봉만" 이라고 남아있던 걸 발견해서 수정. 문서가 코드와 어긋나지 않는지 커밋마다 같이 확인할 것.
- **음봉의 큰 거래대금도 표시** (`1c88d59`) — 그동안 양봉만 동그라미 표시 대상이었는데, 2번째·3번째 구간 모두 실제 최대 거래량이 음봉에서 나왔던 걸 놓치고 있었음. 이제 구간 안에서는 양봉/음봉 상관없이 거래량 기준으로 표시.
- **DOMAIN.md + cv-detection-tuning 스킬 추가** (`985f93d`) — "물량 털기" 관련 트레이딩 개념과 임계값 근거를 `DOMAIN.md`에 정리하고, 검출 로직을 튜닝할 때 실제 캡처+시각적 확인을 거치도록 하는 절차를 스킬로 고정.
- **꼬리 인식 방식을 밝기 최솟값 기준으로 재수정** (`9417539`) — 지난번 수정도 실제로는 부족했음(꼬리가 옅게 렌더링돼서 색상/평균밝기로는 여전히 놓침). 캔들 폭 안에서 가장 어두운 픽셀 기준으로 바꿔서, 고해상도 캡처로 확인한 실제 63px/127px짜리 긴 꼬리까지 정확히 복원되는 것 확인.
- **윗꼬리(고가)까지 포함해서 표시** (`ef83e81`) — 이평선 제거용 필터가 얇은 꼬리도 같이 지워버려서 그동안 고가가 항상 종가와 같게 나왔던 버그를 발견/수정. 이제 종가가 아니라 실제 찍은 최고가까지 동그라미에 포함됨 (윗꼬리가 실제 물량 털기 지점이라는 설명 반영).
- **구간 내 2차 거래량 급증도 함께 표시** (`9d1f6e6`) — 제일 큰 거래량 캔들 하나만 표시하던 걸, 같은 물량 털기 구간(가격이 폭발 캔들 시가 아래로 무너지기 전까지) 안에서 그 절반 정도 크기라도 눈에 띄는 거래량 캔들이 있으면 전부 각각 동그라미로 표시하도록 변경.
- **재접근-하락(물량 털기) 정황 탐지 추가** (`6a453e7`) — 거래량 폭증 캔들이 만든 고점('전고')을 이후 캔들이 다시 찍고 그 밑으로 하락하는지까지 확인. 발견되면 기존 노란 동그라미(폭증 지점)에 주황색 동그라미+연결선으로 재접근 지점을 표시. 캔들 이미지 경로만 지원 (캔들 JSON 경로는 아직 TODO). 표시하는 동그라미도 구간 전체가 아니라 실제 거래량 최대 캔들 하나에 딱 맞게 그리도록 변경.
- **"물량 털기" 용어로 통일** (`371cf89`) — 판단 기준/결과 문구를 일반적인 "가격 상승+거래량 폭증" 대신 세력의 물량 털기 의심 신호로 표현 (탐지 로직 자체는 변경 없음).
- **검출 동그라미 선 두께 축소** (`49976c1`) — 3px → 2px, 차트가 덜 가려지도록.
- **커밋 히스토리 로그 도입** (`f58446a`) — 이 `HISTORY.md`와, 커밋할 때마다 이걸 갱신하는 걸 표준 습관으로 만드는 스킬(`.claude/skills/update-history-log/`) 추가.
- **이미지 클릭 시 확대 모달** (`39aee04`) — 웹 대시보드에서 차트/검출 결과 이미지를 클릭하면 화면에 크게 띄워서 볼 수 있음. "분석하기"로 단일 카드 분석을 완료하면 자동으로 확대 모달이 열림.
- **검출 구간에 동그라미 표시** (`15a095e`) — OpenCV가 찾은 "가격 상승+거래량 폭증" 구간을 캔들/거래량 영역에 동그라미로 표시. 실제 사용자가 손으로 동그라미 친 차트와 위치가 일치함을 확인. CLI는 리포트와 함께 PNG로 저장, 웹은 결과에 이미지로 포함.
- **API 키 없을 때 CLI가 깔끔하게 실패** (`982bd1c`) — 트레이스백 대신 사람이 읽을 수 있는 안내 메시지로 종료.
- **OpenCV 기반 무료 실시간 판단 + 로컬 웹앱** (`3f50069`) — 일봉/30분봉/3분봉의 "가격 상승+거래량 폭증" 판단을 Claude 호출 없이 OpenCV로 직접 계산(무료, 즉시). 화면 캡처는 계속 Claude Vision 사용. 이미지/캔들 데이터(JSON) 입력을 공통 인터페이스(`ChartInput`)로 통일해서 나중에 증권사 REST API 연동 시에도 같은 파이프라인을 재사용 가능. FastAPI + 브라우저 대시보드(`webapp/`) 추가 — 로컬에서 이미지 업로드/붙여넣기로 바로 테스트 가능.
- **최초 스캐폴딩: 일봉/30분봉/3분봉/화면캡처 4개 분석 에이전트** (`4e9070c`) — 타임프레임별 판단 기준을 `config/criteria.yaml`에서 관리하고, Claude Vision으로 차트 이미지를 분석해 종합 리포트를 만드는 CLI 도구(`main.py`)의 기본 구조.
